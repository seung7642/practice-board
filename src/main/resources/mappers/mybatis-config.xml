<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/> <!-- DB의 필드명은 reg_date와 같이 작성하지만, Java 코드에서는 카멜 표기법으로 사용하기 때문에 그걸 맞추기 위한 설정 -->
    </settings>
</configuration>